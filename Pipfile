[[source]]

url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"


[packages]

requests = ">=2.32,<3.0"
"pushbullet.py" = ">=0.12,<1.0"
python-dotenv = ">=1.0,<2.0"
flask = ">=3.1,<4.0"
flask-sqlalchemy = ">=3.1,<4.0"
ccxt = ">=4.0,<5.0"

[dev-packages]
pytest = ">=8.0,<9.0"
mypy = ">=1.10,<2.0"
isort = ">=5.13,<6.0"
flake8 = ">=7.0,<8.0"



[requires]

python_version = "3"
